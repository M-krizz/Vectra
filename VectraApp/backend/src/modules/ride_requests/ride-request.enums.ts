export enum RideRequestStatus {
  REQUESTED = 'REQUESTED',
  MATCHING = 'MATCHING',
  POOLED = 'POOLED',
  ACCEPTED = 'ACCEPTED',
  EXPIRED = 'EXPIRED',
  CANCELLED = 'CANCELLED',
}

export enum RideType {
  SOLO = 'SOLO',
  POOL = 'POOL',
}

export enum VehicleType {
  BIKE = 'BIKE',
  AUTO = 'AUTO',
  CAB = 'CAB',
}
